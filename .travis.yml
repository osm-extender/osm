language: ruby
rvm:
  - 2.2.2
  - 2.2.3
  - 2.2.4
  - 2.3.0
gemfile:
  - Gemfile
branches:
  only:
    - master
    - staging
    - /gh(?:\d)+(?:-.+)?/
    - /dev_ver_\d+\.\d+/
before_install: gem update bundler
script: rake ci:travis
